Fix the Interactive Map in my Red Dead Redemption 2 guide.

Right now the map is NOT truly interactive. When a user clicks a location, it only shows the town name. I want you to rebuild the map interaction so it behaves like a real interactive guide map.

Your task:
1. Find the map component, JavaScript, HTML, and CSS responsible for the current map behavior.
2. Diagnose why clicks only show the town name and why no real interactivity exists.
3. Refactor the feature so each clickable map location opens a rich interactive info panel, modal, tooltip card, or side panel.
4. Each map marker/location must support:
   - Location name
   - Region/state
   - Description
   - Category (town, mission, shop, collectible, legendary animal, stranger mission, etc.)
   - Optional image if available
   - Optional checklist/completion state
   - Optional link/button to related guide section
5. Add proper hover states, active states, and selected-marker styling so users can clearly see what they are interacting with.
6. Make the map support:
   - Hover preview
   - Click to open full details
   - Click another marker to switch details
   - Close button for opened details
   - Mobile-friendly interaction
7. If the current map uses static image coordinates, keep that system if necessary, but make the UX actually interactive.
8. If markers are hardcoded, convert them into structured data such as an array of objects so the map can be rendered dynamically.
9. Make sure the code is clean, modular, and easy to expand with more RDR2 locations later.
10. Do not break the current visual style of the guide. Improve it while keeping the existing theme.

Implementation requirements:
- Use plain JavaScript, HTML, and CSS unless the project already uses a framework.
- Rebuild the marker rendering so markers are generated from data.
- Add event listeners for hover, click, keyboard accessibility, and touch interaction.
- Create a details panel that updates dynamically based on the clicked marker.
- Add accessibility improvements: keyboard focus, aria labels, and semantic buttons for markers.
- Make sure it works on desktop and mobile.
- If there is an existing bug caused by event propagation, wrong selectors, missing data bindings, or overlay layering issues, fix those too.

Expected result:
- The map should no longer only show a town name.
- Clicking a marker should reveal useful guide information.
- The map should feel like a real game guide feature, not a static image with labels.
- The code should be production-ready and scalable.

After fixing it:
1. Show me exactly which files you changed.
2. Explain the root cause of the problem.
3. Summarize the improvements you made.
4. If needed, create or update the marker data structure.
5. If there are parts of the current codebase that block better interactivity, refactor them.

If the current implementation is too broken, replace it with a better architecture instead of patching it superficially.
